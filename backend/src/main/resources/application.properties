spring.application.name=uninex
spring.data.mongodb.uri=${MONGO_URI:mongodb://localhost:27017/uninex}
server.port=${PORT:8080}

# Google OAuth2 login (Issue #5). Real credentials come from environment
# variables so they never live in source control or in this chat -- the
# demo-* fallback lets the app and its tests boot without them, but login
# will only actually work once GOOGLE_CLIENT_ID / GOOGLE_CLIENT_SECRET are
# set to real values from Google Cloud Console (see README "Auth setup").
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID:demo-client-id}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET:demo-client-secret}
spring.security.oauth2.client.registration.google.scope=email,profile

# Origin(s) allowed to call this API with credentials (comma-separated).
# Defaults to the local Vite dev server; set FRONTEND_URL in production to
# the real deployed frontend origin (see SecurityConfig). CORS only cares
# about the origin (scheme+host+port), not any path, so this is safe to
# leave as a bare origin.
app.frontend-url=${FRONTEND_URL:http://localhost:5173}

# Where to send the browser after a successful Google login (see
# SecurityConfig#defaultSuccessUrl). This is deliberately separate from
# app.frontend-url above: FRONTEND_URL is a bare origin for CORS, but a
# GitHub Pages *project* site (as opposed to a user/org site) serves the
# app from a subpath named after the repo -- e.g.
# https://<user>.github.io/<repo>/ -- so the redirect target needs that
# path and FRONTEND_URL alone is not enough. Falls back to FRONTEND_URL's
# first origin if unset, which is correct for local dev and for a
# user/org GitHub Pages site (served from the bare domain).
app.frontend-login-redirect-url=${FRONTEND_LOGIN_REDIRECT_URL:${FRONTEND_URL:http://localhost:5173}}
